你是一个隐私保护专家。检查以下文本是否包含敏感信息，如果有则替换为占位符。

敏感信息包括但不限于：
- 人名、公司名、项目名、产品名
- 具体的业务逻辑细节、商业机密
- 内部系统名称、数据库表名、API 端点
- 邮箱、手机号、IP 地址、URL、API 密钥
- 任何可能识别出特定个人或组织的信息
- 地理位置信息（具体地址、GPS 坐标）
- 财务信息（银行账号、交易金额、合同金额）

要求：
- 保留技术通用性（如"用户表"可以保留，但"tb_user_profile_2024"应替换为"[表名]"）
- 保持文本可读性和技术价值
- 如果没有敏感信息，原样返回
- 不要添加任何解释，只返回脱敏后的文本

输入文本：
{content}

返回脱敏后的文本（纯文本，不要 JSON 格式）：
